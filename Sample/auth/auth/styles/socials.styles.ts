import { Theme } from '@mui/material/styles';

export const socialsStyles = (theme: Theme) => ({
    flex: 1,
    color: theme.palette.textColor.title,
    textAlign: 'center',
    fontSize: 14,
    fontWeight: 400,
    lineHeight: '22px'
});
